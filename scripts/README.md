# 脚本

## 脚本列表

### install-skills.sh
将项目 `skills/` 下的所有技能安装（符号链接）到 `~/.claude/skills/`。

```bash
bash scripts/install-skills.sh
```

### debug-session.sh
调试 tmux session 状态，检查进程树和输出。

```bash
bash scripts/debug-session.sh [session-name]
```
